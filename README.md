# Glass Data Recorder

一个简单的浏览器端数据记录器，支持任务、记录和链接的快速输入与管理。

## 运行方式

1. 直接打开 `index.html`。
2. 或者使用本地开发服务器：

```bash
npm install
npm start
```

## 功能

- 输入任务、记录或链接并按回车保存
- 本地 `localStorage` 持久化数据
- 任务和链接过滤
- 拖拽排序
- 删除和完成标记
