{
  "name": "glass-data-recorder",
  "version": "0.1.0",
  "private": true,
  "description": "A glass-style browser task and link recorder.",
  "scripts": {
    "start": "npx live-server --port=5501 --open=index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
